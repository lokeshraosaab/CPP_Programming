#include<iostream>
using namespace std ;

int main()
{
int a=4;
float b=3.3;
char c='u';
// here = is used to assign values to variables a, b, c so(=) is called assignment operator.
cout<<"The value of the variables a, b, and c is "<<a<<", "<<b<<", and "<<c<<" respectively.";
     return 0;
}