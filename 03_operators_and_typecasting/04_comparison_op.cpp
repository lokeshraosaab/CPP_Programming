#include<iostream>
using namespace std ;

int main()
{
int a=4, b=5;
// if the given comparison is true it returns 1 and if the given comparison is false it returns 0.
cout<<"The value of the comparison (a==b) is "<<(a==b)<<endl;
cout<<"The value of the comparison (a!=b) is "<<(a!=b)<<endl;
cout<<"The value of the comparison (a>=b) is "<<(a>=b)<<endl;
cout<<"The value of the comparison (a<=b) is "<<(a<=b)<<endl;
cout<<"The value of the comparison (a>b) is "<<(a>b)<<endl;
cout<<"The value of the comparison (a<b) is "<<(a<b)<<endl;
     return 0;
}